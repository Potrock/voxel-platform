import { defineMeta } from '@platform';

/** Dev preview: Blockfront's arsenal on display (`?game=bf-arsenal`; never in production builds). */
export default defineMeta({
  id: 'bf-arsenal',
  title: 'Blockfront II: arsenal',
});
