import { defineMeta } from '@platform';

/** Dev preview: Blockfront's maps on their own (`?game=bf-map`; never in production builds). */
export default defineMeta({
  id: 'bf-map',
  title: 'Blockfront II: map',
});
